// Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
// This source file is part of the Cangjie project, licensed under Apache-2.0
// with Runtime Library Exception.
//
// See https://cangjie-lang.cn/pages/LICENSE for license information.

// (CJNATIVE and Linux) ASSERT: regex ^ld64.lld$
// (CJNATIVE and Linux) ASSERT: regex ^ld.lld$
// (CJNATIVE and Linux) ASSERT: regex ^llc$
// (CJNATIVE and Linux) ASSERT: regex ^lld$
// (CJNATIVE and Linux) ASSERT: regex ^lld-link$
// (CJNATIVE and Linux) ASSERT: regex ^lli$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-addr2line$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-ar$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-as$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-cov$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-dis$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-link$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-lto$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-lto2$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-objdump$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-otool$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-profdata$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-profgen$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-symbolizer$
// (CJNATIVE and Linux) ASSERT: regex ^llvm-objcopy$
// (CJNATIVE and Linux) ASSERT: regex ^opt$
