// Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
// This source file is part of the Cangjie project, licensed under Apache-2.0
// with Runtime Library Exception.
//
// See https://cangjie-lang.cn/pages/LICENSE for license information.

// DEPENDENCE: ../%n
// Test: Touch test file without content change - nothing should recompile (early cutoff)
// (Windows)EXEC: copy NUL output
// (Unix)EXEC: cd %n && touch output
// EXEC: cd %n && %cjpm test --no-run --no-feature-deduce --enable-features=user.cmp.arch.little
// EXEC: cd %n && sleep 1 && touch multiplatform/platformB/p_test.cj && %cjpm test --no-run -i -V --no-feature-deduce --enable-features=user.cmp.arch.little 2>&1 > output
// EXEC: cd %n && %cat output | compare %f
// ASSERT: scan-not Compiling `./common/nonproduct` part of package `cmp`
// ASSERT: scan-not Compiling `./platformA/product` part of package `cmp`
// ASSERT: scan-not Compiling `./platformB/product` part of package `cmp`
// ASSERT: scan-1 cjpm test success
