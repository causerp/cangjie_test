# Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
# This source file is part of the Cangjie project, licensed under Apache-2.0
# with Runtime Library Exception.
#
# See https://cangjie-lang.cn/pages/LICENSE for license information.

// DEPENDENCE: ../%n
// EXEC: cd %n && %lint -f ./src  -c %lintConfigDir -m %lintModuleDir 2>&1 | compare %f
// ASSERT: regex-1 a.cj:15:5:.*The nested synchronized objects has a different sequence than the nested synchronized objects on the line 11 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 test.cj:19:5:.*The nested synchronized objects has a different sequence than the nested synchronized objects on the line 15 of the file 'a.cj', which may cause a deadlock.
// ASSERT: regex-1 a.cj:25:3:.*The nested synchronized objects has a different sequence than the nested synchronized objects on the line 15 of the file 'a.cj', which may cause a deadlock.
// ASSERT: regex-1 test.cj:28:3:.*The nested synchronized objects has a different sequence than the nested synchronized objects on the line 11 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 test.cj:28:3:.*The nested synchronized objects has a different sequence than the nested synchronized objects on the line 19 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 test.cj:28:3:.*The nested synchronized objects has a different sequence than the nested synchronized objects on the line 25 of the file 'a.cj', which may cause a deadlock.
