// EXEC: %compiler %cmp_opt %f  -o %output
// RUN-EXEC: %run %run_opt %output %run_args

var a1: (Int32, Int32) -> Int32 = {a, b => a + b}
var a2: (Int32, Int32) -> Int32 = {a: Int16, b => a + b}
main():Int64 {
    if ( a1(1,1) != 2 ) {
        return 1
    }
    if ( a2(1,1) != 2 ) {
        return 1
    }
    return 0
}

