[root]
  path = ../../
[suffix]
  cj = //
  info = //
[env]
  LD_LIBRARY_PATH=./:$LD_LIBRARY_PATH
[inherit]
  inherit=./common.cfg

[condition]
  condition = never
[internal-var]
  # dylib suffix on host machine.
  host_dylib_suffix = so
  mkdir = mkdir -p
  rmdir = rm -rf
  cangjie-repo-token=$CANGJIE_REPO_TOKEN
  repo_sed = sed -i

[description]
  title = Cangjie2cjnative Test
