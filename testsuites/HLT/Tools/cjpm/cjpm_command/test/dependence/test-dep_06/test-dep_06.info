/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
 * This source file is part of the Cangjie project, licensed under Apache-2.0
 * with Runtime Library Exception.
 * 
 * See https://cangjie-lang.cn/pages/LICENSE for license information.
 */

// LEVEL: 2
// DEPENDENCE: ../%n
// EXEC: cd %n/devDep0 && git init && git add . && git -c user.name='name' -c user.email='' commit -am "foo"
// (x86 and not Windows) EXEC: cd %n && %git_dir_prefix%n%git_dir_suffix %cjpm test -V src && %ls target/release 2>&1 | compare %f
// (x86 and not Windows) EXEC: cd %n && %git_dir_prefix%n%git_dir_suffix %cjpm check 2>&1 | compare check_result.txt
// (x86 and not Windows) EXEC: cd %n && %git_dir_prefix%n%git_dir_suffix %cjpm tree 2>&1 | compare tree_result.txt
// (x86 and not Windows) ASSERT: scan devDep0
// (x86 and not Windows) ASSERT: scan devDep1
// (x86 and not MAC and not Windows) ASSERT: scan test
