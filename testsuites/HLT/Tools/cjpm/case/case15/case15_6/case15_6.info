/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
 * This source file is part of the Cangjie project, licensed under Apache-2.0
 * with Runtime Library Exception.
 * 
 * See https://cangjie-lang.cn/pages/LICENSE for license information.
 */

// LEVEL: 2
// DEPENDENCE: ../%n
// (native_compilation)EXEC: cd %n/libtest && %clang -c -o test.o test.c && ar -rc libtest.a test.o
// (native_compilation)EXEC: cd %n && %cjpm test -V 2>&1 | compare %f
// (native_compilation & host_win) ASSERT: scan -ldep -lcrypt32 --link-options -L ./libtest -ltest
// (native_compilation & !host_win & !host_mac) scan -ldep -ldl --link-options -L ./libtest -ltest
// (native_compilation & host_mac) ASSERT: scan -ldep -ldl --link-options -L ./libtest -ltest
// ASSERT: scan cjpm test success