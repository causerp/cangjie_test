/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
 * This source file is part of the Cangjie project, licensed under Apache-2.0
 * with Runtime Library Exception.
 * 
 * See https://cangjie-lang.cn/pages/LICENSE for license information.
 */

// LEVEL: 1
// DEPENDENCE: ./src
// EXEC-PIPE: %lint -f ./src  -c %cangjieDir/tools -m %cangjieDir 2>&1 | compare %f
// ASSERT: regex-1 :25:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 25 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :28.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 25 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :31:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 28 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :31:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 31 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :31:104.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 25 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :31:104.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 28 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :34:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 25 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :34:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 28 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :34:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 31 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :34:21.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 34 of the file 'test.cj', which may cause a deadlock.

