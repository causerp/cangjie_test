/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
 * This source file is part of the Cangjie project, licensed under Apache-2.0
 * with Runtime Library Exception.
 * 
 * See https://cangjie-lang.cn/pages/LICENSE for license information.
 */

// LEVEL: 1
// DEPENDENCE: ./src
// EXEC-PIPE: %lint -f ./src  -c %cangjieDir/tools -m %cangjieDir 2>&1 | compare %f
// ASSERT: regex-1 :49:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 30 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :49:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 36 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :55:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 24 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :55:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 36 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :61:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 18 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :61:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 36 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :67:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 12 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :67:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 36 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :73:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 12 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :73:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 18 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :73:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 24 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :73:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 36 of the file 'test.cj', which may cause a deadlock.
// ASSERT: regex-1 :73:.*error.*?The nested synchronized objects has a different sequence than the nested synchronized objects on the line 30 of the file 'test.cj', which may cause a deadlock.
