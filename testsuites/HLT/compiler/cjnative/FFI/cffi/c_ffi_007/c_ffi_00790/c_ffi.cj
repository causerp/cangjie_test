/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2025. All rights reserved.
 * This source file is part of the Cangjie project, licensed under Apache-2.0
 * with Runtime Library Exception.
 * 
 * See https://cangjie-lang.cn/pages/LICENSE for license information.
 */

// LEVEL:2
// DEPENDENCE: %n.c
// (not Windows) EXEC: %clang %clang_opt %n.c -o %cffi_output
// (not Windows) EXEC: %compiler %cmp_opt %ffic_opt -o %output
// (not Windows) RUN-EXEC: %run %run_opt %output %run_args

foreign func testfunc1(): CPointer<Float32>

foreign func testfunc2(): CPointer<Float64>

foreign func free(cp: CPointer<Float64>): Unit

unsafe main(): Int64 {
    var count = 0
    var ptr1: CPointer<Float32> = testfunc1()
    var ptr2: CPointer<Float64> = CPointer<Float64>(ptr1)
    var ptr3: CPointer<Float64> = testfunc2()
    var ptr4: CPointer<Float32> = CPointer<Float32>(ptr3)

    if ((ptr2.read(0) - 2.0 < 1e-6) && (ptr4.read(0) - 0.0 < 1e-6)) {
        free(CPointer<Float64>(ptr1))
        free(ptr3)
        return 0
    } else {
        return 1
    }
}
